# t20x

T20 cricket analytics: opponent-quality-adjusted ratings, expected runs, and cricket WAR.

## Install

```bash
pip install -e ".[dev]"
```

## Quick Start

```bash
# Ingest Cricsheet data (auto-downloads ~100MB)
t20x ingest --league ipl

# Check database status
t20x ingest status
```
